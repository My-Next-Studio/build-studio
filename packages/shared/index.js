const registry = require('./registry');
const processManager = require('./process-manager');
const constants = require('./constants');
const paths = require('./paths');

module.exports = { registry, processManager, constants, paths };
